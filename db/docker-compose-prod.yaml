version: '3.7'

services:
  pg_featureserv:
    image: ${DOCKER_REGISTRY}/caresteouvert-pg_featureserv:latest
  geoip:
    image: ${DOCKER_REGISTRY}/caresteouvert-geoip:latest
  web:
    image: ${DOCKER_REGISTRY}/caresteouvert-back:latest
  worker:
    image: ${DOCKER_REGISTRY}/caresteouvert-back:latest
  front:
    image: ${DOCKER_REGISTRY}/caresteouvert-front:latest
  osm_imposm:
    image: ${DOCKER_REGISTRY}/caresteouvert-imposm:latest
